
interface Course {
    id: number;
    name: string;
    recurring_dates: string; // Adjust the type if needed
    recurrence: string | null; // Adjust the type if needed
    start_time: string;
    end_time: string;
}



